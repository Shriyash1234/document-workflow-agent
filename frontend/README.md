# Frontend

React + Vite frontend for the agentic workflow POC.

## Run

```bash
npm install
npm run dev
```

The app runs on:

```txt
http://localhost:5173
```

The backend must be running on:

```txt
http://localhost:4000
```
